#summary Move to github
#labels Phase-Requirements

= Notice =

The repository have moved to github(https://github.com/chaosim). 

The download files is on pypi(http://pypi.python.org/pypi/daot).

The document is on pypi(http://packages.python.org/daot).